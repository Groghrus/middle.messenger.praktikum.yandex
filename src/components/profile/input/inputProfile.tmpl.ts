const template = `
    <label class="text-dark text-14-500">{{inputLabel}}</label> 
    <input 
      class="input-profile text-grey text-14-500" 
      placeholder="{{inputPlHolder}}" 
      type="{{inputType}}" 
      name="{{inputName}}"
      value="{{inputValue}}"
      > 
`

export default template
